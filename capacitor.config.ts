import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'I3 Condominios',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
